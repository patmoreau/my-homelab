services:
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:10.11.11ubu2404-ls36
    container_name: jellyfin
    ports:
      - 8096:8096
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - JELLYFIN_PublishedServerUrl=${JELLYFIN_PublishedServerUrl}
    volumes:
      - /data/jellyfin:/config
      - ./config/cache:/config/cache
      - ./config/log:/config/log
      - ./config/transcodes:/config/transcodes
      - /media/downloads/complete:/data/downloads:ro
      - /media/holidays:/data/holidays:ro
      - /media/kids:/data/kids:ro
      - /media/les-mills:/data/les-mills:ro
      - /media/movies:/data/movies:ro
      - /media/tv:/data/tv:ro
    restart: unless-stopped