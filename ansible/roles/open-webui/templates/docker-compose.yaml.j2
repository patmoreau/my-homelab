name: open-webui

services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: unless-stopped
    ports:
      - "3000:8080"
    volumes:
      - /data/open-webui/data:/app/backend/data
    environment:
      - OLLAMA_BASE_URL=http://{{ hostvars['lxc-ollama']['ansible_host'] }}:11434
      - TZ={{ timezone }}
