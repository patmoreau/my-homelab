- name: Apply tmpfiles
  ansible.builtin.command: systemd-tmpfiles --create /etc/tmpfiles.d/podman-docker-sock.conf
  changed_when: true

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
