http:
  routers:
    homepage:
      rule: "Host(`home.moreaulab.ca`)"
      entryPoints:
        - websecure
      middlewares:
        - oauth2-auth@file
      tls:
        certResolver: cloudflare
      service: homepage

  services:
    homepage:
      loadBalancer:
        servers:
          - url: "http://{{ hostvars['lxc-tools']['ansible_host'] }}:3000"
